services:
  app:
    volumes:
      - ./dash-auth-flask:/app
      - app-data:/data
    ports:
      - "8050:8000"
    environment:
      APP_ENV: development
    command: >-
      sh -c "flask --app app:server init-db &&
      flask --app app:server seed-demo &&
      python app.py"
